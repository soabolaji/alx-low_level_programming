#include "main.h"
#include <stdio.h>

/**
 * main - function that checks for uppercase character.
 *
 * Return: 1 if c is upper case otherwise 0.
 *
 */

	int _isupper(int c)
	{
	if (c >= 'A' && c <= 'Z')
	return (1);

	else
	
	return (0);
	}
