#include <stdio.h>

/**
 * main - to print its name follow by new line.
 * @argc: number of supplied arguments
 * @argv: an array od arguments
 *
 * Return: Always 0
 *
 */


int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
